<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AcademicPlanner.ViewModel"
             x:Class="AcademicPlanner.View.AssessmentPage">

    <ContentPage.BindingContext>
        <viewModels:AssessmentPageViewModel />
    </ContentPage.BindingContext>
    
    <NavigationPage.TitleView>
        <Button Text="Delete" x:Name="deleteAssessmentButton" Command="{Binding DeleteAssessmentCommand}"/>
    </NavigationPage.TitleView>

    <ContentPage.Content>
        <StackLayout>
            <Button Text="Edit Assessment" Style="{StaticResource editButton}"/>
            <Label x:Name="assessmentIDLabel" IsVisible="False" Text="{Binding AssessmentID, Mode=TwoWay}"/>
            <Label x:Name="courseIDLabel" IsVisible="False" Text="{Binding CourseID, Mode=TwoWay}"/>
            <Entry x:Name="assessmentNameLabel" Text="{Binding AssessmentName}"/>
            <Entry x:Name="assessmentTypeLabel" Text="{Binding AssessmentType}"/>
            <DatePicker x:Name="startDate" Date="{Binding StartDate}"/>
            <DatePicker x:Name="endDate" Date="{Binding EndDate}"/>

            <Button Text="Update Term" Command="{Binding EditAssessmentCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>